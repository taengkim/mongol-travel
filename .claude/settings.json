{
  "permissions": {
    "allow": [
      "WebFetch(domain:www.ktour25.com)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "shell": "powershell",
            "command": "[Console]::OutputEncoding = [System.Text.Encoding]::UTF8; $env:Path = [System.Environment]::GetEnvironmentVariable('Path','Machine') + ';' + [System.Environment]::GetEnvironmentVariable('Path','User'); Set-Location 'd:\\project\\mongol'; git add .; $timestamp = Get-Date -Format 'yyyy-MM-dd HH:mm'; git commit -m \"auto-push $timestamp\"; git push",
            "statusMessage": "GitHub에 자동 push 중..."
          }
        ]
      }
    ]
  }
}
